
export interface timeData {
  checkinDate: string;
  checkinTime: string;
  checkoutDate: string;
  checkoutTime: string;
  userId: string;
  checkin: string;
  checkout: string;
  status: string;
}
